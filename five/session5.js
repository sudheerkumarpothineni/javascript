/*
    Arrays
    ------
    Store diff data types into a single variable

    Common array methods
    --------------------
    let fruits = ["apple", "banana", "guva"];

    push() - add element at the end - fruits.push("orange");
    pop() - removes the last element - fruits.pop();
    unshift() - add the element at the first - fruits.unshift("mosambi");
    shift() - removes the first element - fruits.shift();
    indexOf() - get the index of element - fruits.indexOf("orange");
    includes() - check wheather the value is there in the array or not - fruits.includes("orange");
    join() - convert array to string - fruits.join(",");
    concat() - merge arrays - fruits.concat(array2);
    slice() - copy portion of array - fruits.slice(1,3);
    splice() - add/remove elements - fruits.splice(1,2);
    reverse() - reverse array - fruits.reverse();
    sort() - sort elements - fruits.sort();
*/